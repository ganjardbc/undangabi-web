<template>
  <div id="AppAlert" class="display-popup">
    <div class="card-alert shadow-sm post-middle">
      <div v-if="!isLoader" class="flex justify-content items-center">
        <div style="width: 100%">
          <div class="micro font-semibold">{{ label ? label : 'Alert' }}</div>
        </div>
        <button class="btn btn-small-icon btn-white" @click="onClose">
          <i class="fa fa-lg fa-times"></i>
        </button>
      </div>
      <div class="content-center">
        <div class="p-4 padding-top-30-px padding-bottom-30-px">
          <i
            :class="
              isLoader
                ? 'fa fa-2x fa-spinner fa-spin'
                : 'fa fa-2x fa-info-circle'
            "
            style="font-size: 48px"
          ></i>
        </div>
        <div
          class="micro font-semibold width width-90 width-center margin margin-bottom-30-px"
        >
          {{
            isLoader
              ? enableEnglish
                ? 'Please wait..'
                : 'Mohon tunggu'
              : title
                ? title
                : 'This action will take permanently to datas, are you sure ?'
          }}
        </div>
        <div v-if="!isLoader" class="flex justify-content center">
          <button
            class="btn btn-main btn-full margin margin-right-5-px"
            @click="onSave"
          >
            {{ enableEnglish ? 'Yes, continue' : 'Iya, Lanjutkan' }}
          </button>
          <button
            class="btn btn-grey btn-full margin margin-left-5-px"
            @click="onClose"
          >
            {{ enableEnglish ? 'Cancel' : 'Kembali' }}
          </button>
        </div>
      </div>
    </div>
  </div>
</template>
<script>
export default {
  name: 'AppAlert',
  props: {
    enableEnglish: {
      type: Boolean,
      required: false,
      default: true,
    },
    isLoader: {
      type: Boolean,
      required: false,
    },
    label: {
      type: String,
      required: false,
    },
    title: {
      type: String,
      required: false,
    },
    onSave: {
      type: Function,
      required: false,
    },
    onClose: {
      type: Function,
      required: false,
    },
  },
}
</script>
